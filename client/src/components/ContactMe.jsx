import React, { Component } from "react";
import styled from "styled-components";

const ContactWrapper = styled.div``;

export default class Contact extends Component {
  render() {
    return (
      <ContactWrapper>
        <section className="">Contact Goes Here</section>
      </ContactWrapper>
    );
  }
}
