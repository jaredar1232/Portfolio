import React, { Component } from "react";
import styled from "styled-components";

const FooterWrapper = styled.div``;

export default class Footer extends Component {
  render() {
    return (
      <FooterWrapper>
        <section className="">Footer Goes Here</section>
      </FooterWrapper>
    );
  }
}
