import React, { Component } from "react";
import styled from "styled-components";

const ResumeWrapper = styled.div``;

export default class Resume extends Component {
  render() {
    return (
      <ResumeWrapper>
        <section className="">Resume Goes Here</section>
      </ResumeWrapper>
    );
  }
}
